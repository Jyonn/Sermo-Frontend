# Sermo UI Iteration Todo

说明：
- 本文档用于跟踪当前 UI / 文案 / 交互优化。
- 按优先级排序：`P0 -> P1 -> P2 -> P3`。
- 执行规则：每完成一项，将对应 checkbox 从 `[ ]` 改为 `[x]`。
- 当前阶段仅记录，不预先标记任何未实施项。

## P0 通用优化

### 去原型化与信息清理
- [x] 删除所有开发者暴露信息：如技术栈、实现状态、接口说明、原型说明、系统说明文案。
- [x] 删除顶部不属于真实产品的信息入口，如当前用于演示的全局说明性按钮和标签。
- [x] 删除底部与右栏中面向开发者的说明，例如“接口状态”“实现备注”“已接入真实接口”等。
- [x] 删除所有不对用户决策产生帮助的“实现层解释”文字。
- [x] 删除所有请求 ID、内部状态字样、系统锁定等面向内部排错的信息展示。

### 全局信息层级
- [x] 统一所有页面的信息层级，只保留“主标题 / 次要状态 / 主要动作”三层结构。
- [x] 控制每屏仅有一个主任务，避免同层级出现多个强主操作。
- [x] 统一按钮优先级：主按钮、次按钮、弱操作按钮、危险按钮。
- [x] 统一页面级错误、字段错误、成功反馈、加载反馈的视觉和交互模型。
- [x] 统一空状态结构：一句说明 + 一个明确下一步动作。

### 全局文字取舍
- [x] 删除所有“对开发者有效、对用户无效”的说明性长文案。
- [x] 将所有“技术实现解释”改为用户任务导向的简短文案。
- [x] 把所有不必要的详细时间戳改为相对时间表达。
- [x] 清理页面中重复表达同一状态的文字，例如同时出现在线点、在线文字、心跳描述。
- [x] 建立统一的产品文案语气：短句、直白、可行动，不解释实现。

### 全局样式替代文字
- [x] 用样式区分而不是说明文字区分 `Verified / Basic`。
- [x] 用样式区分而不是说明文字区分 `私聊 / 群聊`。
- [x] 用样式区分而不是说明文字区分 `在线 / 离线 / 未读 / 当前导航`。
- [x] 将重复性的说明标签改为 chip、badge、icon、布局关系表达。
- [x] 建立统一 badge/chip 体系，避免不同页面各自发明状态展示方式。

### 全局结构与导航
- [x] 重做全局页头，使其更像真实产品而不是 IA 演示器。
- [x] 页头只保留品牌、当前页面语义、关键操作，不直接暴露“进入层/会话层/关系层/设置层”这类内部架构词。
- [x] 重做移动端底部导航的视觉优先级与当前态表达。
- [x] 统一列表项点击区域、hover、选中态和焦点态。
- [x] 统一危险操作的视觉与确认逻辑。

## P1 核心流程优化

### Entry 页面
- [x] 删除 Entry 页中的指标卡、实现说明和非必要品牌长文。
- [x] 保留一句品牌主张，其余文案聚焦“当前要完成什么任务”。
- [x] 重做 `创建 / 登录 / 加入` 切换区，使其更像任务切换而不是原型 tab。
- [x] 根据模式动态重排字段顺序，确保最重要字段优先出现。
- [x] 精简每种模式的副标题，控制在一句内。
- [x] 让验证码发送动作更贴近邮箱字段，而不是在视觉上游离。
- [x] 明确验证码发送、倒计时、重发、发送失败的反馈层级。
- [x] 优化 slug 输入体验：输入约束、错误提示、示例值展示。
- [x] 优化昵称与访问密码区域的主次关系，避免等权堆叠。
- [x] 调整 CTA 文案，避免“查看主界面”之类偏演示性质的操作抢主路径。

### 聊天列表
- [x] 重做会话列表项信息结构，只保留：会话名、消息摘要、时间、未读、必要状态。
- [x] 私聊列表项不再展示“私聊·已验证·2位成员”这类复合文本。
- [x] 群聊列表项不使用冗余文字解释类型，优先通过标题和样式区分。
- [x] Verified 状态改为视觉徽标，不再用文字直述“已验证”。
- [x] 在线状态改为在线点或轻量状态提示，不在列表中冗余描述。
- [x] 统一最后消息摘要长度与截断策略。
- [x] 调整时间显示，优先使用“刚刚 / 分钟前 / 昨天 / 月日”。
- [x] 收敛左栏顶部动作入口，避免多个并列按钮破坏焦点。

### 聊天主区头部
- [x] 私聊头部改为“名字 + 在线状态”，不再显示“2位成员”。
- [x] 群聊头部改为“群名 + 成员数”，不显示无意义状态串联文案。
- [x] Verified 状态用 badge 显示，而不是文字说明。
- [x] 在线状态仅保留用户能感知的表达，如“在线 / 刚刚活跃”。
- [x] 移除头部不直接服务当前会话任务的冗余信息。

### 消息区
- [x] 优化消息时间展示策略，减少每条消息都显示时间的噪声。
- [x] 强化连续消息分组，减少重复头像和重复元信息。
- [x] 优化系统分隔信息如“今天 / 新消息”的视觉层级。
- [x] 确保“自己 / 对方”通过对齐和气泡样式区分，而不是额外文案。
- [x] 调整消息区背景与层级，让主体内容更聚焦。

### 输入区
- [x] 优化输入框聚焦态和悬浮感，增强可写性暗示。
- [x] 明确发送按钮启用/禁用/发送中状态。
- [x] 优化多行输入后的高度增长节奏。
- [x] 收敛附件、扩展、发送按钮的层级和间距。
- [x] 保证 Enter / Shift+Enter 行为的可预期性与反馈一致性。

### 聊天右栏
- [x] 删除右栏中的实现说明、接口说明、技术状态。
- [x] 私聊右栏仅保留：头像、名字、Verified、在线状态、好友关系、核心动作。
- [x] 群聊右栏仅保留：群名、成员、邀请/管理入口、退出等动作。
- [x] 把长段说明文字改成简洁字段或操作卡片。
- [x] 重做右栏的卡片顺序，让最常用动作前置。

## P2 关系层优化

### 好友页
- [x] 重做 `Incoming / Outgoing / Friends` 三态的视觉与交互差异。
- [x] Incoming 卡片突出“同意 / 拒绝”两个决策操作。
- [x] Outgoing 卡片改为“等待处理 / 可撤回”的语义表达。
- [x] Friends 列表改为“人 + 状态 + 快速动作”的低认知成本结构。
- [x] 移除好友申请中的请求 ID、内部状态值、系统说明。
- [x] Verified 用户在好友页中使用视觉徽标表达，而不是“Verified 用户”文本。
- [x] 重新设计好友限制态文案，使其更像升级引导而不是规则说明。
- [x] Basic 用户受限时，优先展示“为什么受限 + 升级能获得什么”。

### Space 用户页 / 在线用户页
- [x] 从“数据卡片”转向“发现人”的展示方式。
- [x] 删除最近心跳精确时间戳展示。
- [x] 保留在线状态，但改用用户可理解的轻量表达。
- [x] 强化头像、昵称、状态、动作之间的视觉关系。
- [x] 压缩或截断 bio，避免信息过长稀释动作焦点。
- [x] 让“发起私聊”成为主操作，“好友申请”作为次操作。
- [x] Basic 用户发起好友申请失败时，展示产品化限制提示而不是直接暴露错误语句。
- [x] 在线页强化实时感，但避免强闪烁和噪声动画。

## P3 设置层优化

### 账号升级页
- [x] 重做账号升级页，使其更像价值转化页而不是单纯表单页。
- [x] 清晰区分当前身份状态与升级后收益。
- [x] 把“发送验证码 -> 输入验证码 -> 设置密码 -> 升级成功”整理成更清晰的流程感。
- [x] 优化升级成功反馈，强化身份变化结果。

### 通知偏好页
- [x] 从“参数设置卡片”转向“通知场景配置”表达。
- [x] 保留渠道卡片结构，但让核心问题聚焦为“什么时候提醒我”。
- [x] 弱化对后端字段的直接映射感。
- [x] 统一各渠道卡片的布局节奏和反馈方式。

### 联系方式绑定页
- [x] 明确每个渠道的当前状态：已绑定 / 未绑定 / 待验证。
- [x] 弱化“表单工具感”，增强“安全绑定流程感”。
- [x] 让发送验证码和确认绑定形成更明确的两步关系。
- [x] 优化绑定成功/失败后的页面级反馈。
- [x] 若后端暂不返回已绑定目标值，则设计为合理的占位状态而不是裸缺失。

## P4 移动端与细节打磨

### 移动端
- [x] 调整移动端页头，使其更符合单手使用场景。
- [x] 优化移动端底部导航与页面标题的关系。
- [x] 重新检查移动端卡片密度和留白，避免桌面结构简单压缩。
- [x] 保证移动端消息输入区、主要 CTA、列表切换都符合单手操作路径。

### 动效与细节
- [x] 统一页面切换、列表更新、消息到达、操作成功的动效语言。
- [x] 减少当前“同一种动画 everywhere”的感觉，按场景分配动效。
- [x] 统一 icon 语义和图标使用规则。
- [x] 统一相对时间、空状态、成功反馈、危险确认的全局语言。
- [x] 做一轮长昵称、空消息、无好友、无聊天、受限账号等边界场景视觉 QA。

## 执行顺序

1. [x] 完成 P0 通用优化
2. [x] 完成 P1 核心流程优化
3. [x] 完成 P2 关系层优化
4. [x] 完成 P3 设置层优化
5. [x] 完成 P4 移动端与细节打磨

## P5 主导航与信息架构重构

### 四分区主导航
- [x] 将底部 Tab 重构为 `聊天 / 广场 / 通知 / 菜单` 四个一级入口。
- [x] 保留旧的好友、成员、设置页面，但将它们归为 `广场` 或 `菜单` 体系下的二级入口。
- [x] 统一一级导航的样式语言，使其更接近规范化网页类 APP，而不是页面索引器。

### 聊天
- [x] 保留聊天页搜索框，并让聊天页继续作为默认主入口。
- [x] 将聊天页顶部的“找人”动作切换到 `广场`，形成更清晰的信息架构。

### 广场
- [x] 新增 `广场` 主页面，强调发现人而不是查看数据。
- [x] 为广场加入浮动 avatar 动画舞台，形成更接近“广场”的氛围感。
- [x] 让广场同时提供在线成员发现和推荐浏览两种视图，但保持页面清爽。
- [x] 将成员详情和次要动作收进 bottom sheet，避免主屏信息过载。

### 通知
- [x] 新增 `通知` 主页面，整合未读聊天、好友申请和提醒摘要。
- [x] 把通知页做成轻量的提醒中心，而不是把所有设置直接暴露在主屏。
- [x] 将好友申请处理放入二级 bottom sheet，保持通知主列表可扫读。

### 菜单
- [x] 新增 `菜单` 主页面，用来承接账号、偏好、关系、空间和会话安全入口。
- [x] 将菜单按 `身份 / 偏好 / 关系 / 会话` 分组，减少移动端首屏认知负担。
- [x] 避免在菜单首屏直接展开复杂表单，只保留清晰的分组入口和必要摘要。

## P6 去卡片化重排

### 全局
- [x] 移除一级页面中大部分解释性卡片、提示卡片和信息英雄区。
- [x] 将主页面统一收敛为 `标题 / 搜索或切换 / 列表主体` 的排版结构。
- [x] 把次要说明和低频操作收进二级 sheet，而不是留在主屏。

### 聊天
- [x] 将聊天首页改为按最后聊天时间排序的会话列表。
- [x] 取消聊天首页里的重复说明、引导按钮和“最近聊天”式卡片容器。
- [x] 将具体聊天内容只放在进入某个会话后展示。
- [x] 让聊天详情页更接近即时通讯产品，而不是带大量外层包装。

### 广场 / 通知 / 菜单
- [x] 将广场改成搜索 + 动态头像场 + 成员列表的轻量结构。
- [x] 将通知页改成收件箱式列表，不再展示摘要卡片和说明区块。
- [x] 将菜单页改成标准入口列表，不再使用大块 profile hero 和说明卡片。

### 二级页
- [x] 将好友页改成 tab + 列表，去掉 guide、限制说明常驻卡片。
- [x] 将成员页改成搜索 + 列表，去掉 tips 和说明侧栏。
- [x] 将设置页改成标准设置结构，去掉 hero、收益卡、流程卡的大面积占屏。

## P7 入口拆分

### URL 与入口规则
- [x] 将管理员入口收敛为 `/space`。
- [x] 将成员加入入口收敛为 `/space/:slug`。
- [x] 保留 `/entry`、`/space/create`、`/space/login`、`/space/join` 作为兼容跳转。
- [x] 支持从三级域名自动识别 slug，并落到成员加入入口。
- [x] 在成员加入页提供管理员登录跳转，并自动带上 slug。

### 页面拆分
- [x] 将原来的统一入口页拆成“管理员页”和“成员加入页”两个页面。
- [x] 管理员页承载 `创建 Space / 管理登录` 两种 flow。
- [x] 成员加入页不再展示 slug 输入框，只保留昵称和可选访问密码。
